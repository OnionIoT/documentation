Autoboot will be disabled and the bootloader command line will be visible in the serial command line output.

<!-- TODO: add screenshot of console output when bootloader command line is activated - add this when boot menu is removed -->